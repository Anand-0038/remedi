diff --git a/dbt/models/orders_amount_compat.sql b/dbt/models/orders_amount_compat.sql
new file mode 100644
--- /dev/null
+++ b/dbt/models/orders_amount_compat.sql
@@ -0,0 +1,17 @@
+{{ config(materialized='view', tags=['remedi', 'schema-compat']) }}
+
+-- Generated by Remedi for incident: schema-orders-amount
+-- Grounded in DataHub URN: urn:li:dataset:(urn:li:dataPlatform:snowflake,retail.orders,PROD)
+-- Schema columns: order_id, amount, ordered_at
+-- Observed query (DataHub):
+-- SELECT amount::NUMBER(10,2) FROM retail.orders LIMIT 10
+
+-- Upstream widened NUMBER(10,2) -> NUMBER(18,4); protect downstream casts.
+
+SELECT
+  order_id,
+  ordered_at,
+  amount AS amount_native,
+  amount::NUMBER(10,2) AS amount_legacy,
+  amount::NUMBER(18,4) AS amount_full
+FROM {{ source('retail', 'orders') }}
diff --git a/docs/schema_change_notice.md b/docs/schema_change_notice.md
new file mode 100644
--- /dev/null
+++ b/docs/schema_change_notice.md
@@ -0,0 +1,16 @@
+# Schema change notice — retail.orders
+
+## Change
+Column `amount` widened from `NUMBER(10,2)` to `NUMBER(18,4)` (types from DataHub / incident details).
+
+## Blast radius (2 assets)
+- mart_daily_revenue (dataset): Casts amount to NUMBER(10,2)
+- Finance GMV (dashboard): Dashboard metric uses mart_daily_revenue.amount
+
+## Owners to notify
+- urn:li:corpuser:finance-eng
+- urn:li:corpuser:finance-lead
+- urn:li:corpuser:retail-platform
+
+## Recommended action
+Merge the compat view, update Looker explores to `amount_full`, then remove `amount_legacy`.
