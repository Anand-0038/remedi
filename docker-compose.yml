services:
  remedi:
    build: .
    ports:
      - "8790:8790"
    environment:
      REMEDI_MODE: fixture
      HOST: 0.0.0.0
      PORT: 8790
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./examples:/app/examples
